class BasicSqlController < ApplicationController
  def index
    #render 'new'
    #redirect_to :action => :contact
  end
  def loadfile
    #@text=""
    #File.extname(params[:xml_file].original_filename)
    #uploaded_xml_string = params[:textfile].read
    #File.open("/home/siraj/Desktop/RecordsFile.rb", "r").each_line do |line|
     # name: "Angela"    job: "Writer"    ...
   # data = uploaded_xml_string.split(/\t/)
    #@text+=line
    #render :text => uploaded_xml_string
    #name, job = data.map{|d| d.split(": ")[1] }.flatten
    #end
  end
  def insert
  end
end
